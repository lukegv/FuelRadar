<?xml version="1.0" encoding="utf-8" ?>
<CarouselPage xmlns="http://xamarin.com/schemas/2014/forms"
              xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              xmlns:converter="clr-namespace:FuelRadar.UI.Converter"
              x:Class="FuelRadar.UI.FavoritesPage"
              ItemsSource="{Binding Favorites}" >
  <CarouselPage.Resources>
    <ResourceDictionary>
      <converter:BrandToIconConverter x:Key="BrandToIcon" />
    </ResourceDictionary>
  </CarouselPage.Resources>
  <CarouselPage.ItemTemplate>
    <DataTemplate>
      <ContentPage>
        <Grid Padding="10">
          <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
          </Grid.RowDefinitions>
          <!--Header-->
          <Grid Grid.Row="0" Grid.ColumnSpacing="10">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="auto" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Image Grid.Column="0" Source="{Binding Brand, Converter={StaticResource BrandToIcon}}" Scale="2" />
            <Label Grid.Column="1" Text="{Binding Name}" FontSize="24" LineBreakMode="TailTruncation" />
          </Grid>
        </Grid>
      </ContentPage>
    </DataTemplate>
  </CarouselPage.ItemTemplate>
</CarouselPage>